<h1>All bloggers</h1>

<% @users.each do |user| %>
  <p><%=  link_to gravatar_for(user), user_path(user) %></p>
    <p><%= user.email %></p>
    <p><%= link_to user.username , user_path(user) %></p>
    <p><%= pluralize(user.articles.count , "Article") if user.articles %></p>
<% end %>